train <- read.csv("train.csv", header = TRUE)
test <- read.csv("test.csv", header = TRUE)

estimates <- function(n){
  len <- length(n)
  
  ret <- list()
  
  retval$mu <- sum(n)/len
  temp <- sum(n)-
  retval$si_ml <- 
}
  